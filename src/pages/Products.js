import React from "react";
import ProductsList from "../components/ProductsList";

const Products = () => {
  return (
    <div>
      <ProductsList />
    </div>
  );
};

export default Products;
